name: Cucumber UI Tests Only (Allure)

on:
  push:
    branches: [ main, master ]
    paths:
      - 'final/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'final/**'

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  NODE_VERSION: '20.x'
  BUILD_FOLDER: ./final

jobs:
  test-ui:
    name: Run UI Tests Only
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ${{ env.BUILD_FOLDER }}/package-lock.json

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        working-directory: ${{ env.BUILD_FOLDER }}
        run: npm ci

      - name: Generate API Models
        working-directory: ${{ env.BUILD_FOLDER }}
        run: npm run generate:models

      - name: Install Playwright Browsers
        working-directory: ${{ env.BUILD_FOLDER }}
        run: npx playwright install --with-deps
      
      - name: Run UI tests
        working-directory: ${{ env.BUILD_FOLDER }}
        run: npx cross-env ENV=qa npm run test:base -- --tags "@ui and not @api"
        continue-on-error: true

      - name: Generate Allure Report
        if: always()
        working-directory: ${{ env.BUILD_FOLDER }}
        run: npx allure generate allure-results --clean -o allure-report

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-html-report
          path: ${{ env.BUILD_FOLDER }}/allure-report
          retention-days: 14